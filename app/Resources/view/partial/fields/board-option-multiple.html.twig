{% set optionName %}
    {% if tableColNum is not null %}
        {{ parent ~ '[' ~ key ~ ']' }}
    {% else %}
        {{ parent ~ '[' ~ key ~ ']' }}
    {% endif %}
{% endset %}
{% set optionValue %}
    {% if tableColNum is not null %}
        {{ tableColNum }}
    {% else %}
        {{ textNum|number_format - parent|split('t')[1]|number_format }}
    {% endif %}
{% endset %}

v{{ optionValue }}
{% include 'partial/fields/board-input.html.twig' with { 'inputCss': 'f6 f5-l pointer bn mt1', 'type': 'checkbox', 'value': value|default(optionValue), 'name': name|default(optionName), 'id': id|default(parent ~ '-' ~ key) } %}
