{% set parentTextNum = parent|split('t')[1]|number_format + 0 %}

{% set optionValue %}
    {% if tableColNum is not null %}
        {{ tableColNum }}
    {% else %}
        {{ textNum - parentTextNum }}
    {% endif %}
{% endset %}

v{{ optionValue }}
{% include 'partial/fields/board-input.html.twig' with { 'inputCss': 'f6 f5-l pointer bn mt1', 'type': 'radio', 'value': value|default(optionValue), 'name': name|default(parent), 'id': id|default(parent ~ '-' ~ key) } %}