{% set extraAttrs = type|default('text') is same as('text') ? 'minLength="' ~ minLength|default(0) ~ '" maxLength="' ~ maxLength|default(100) ~ '" placeholder="' ~ placeholder|default(textPlaceholder|default) ~ '" autocomplete="off"' : checked|default %}
{% set extraAttrs = extraAttrs ~ ' ' ~ disabled|default ~ ' ' ~ readonly|default ~ ' ' ~ required|default %}
<input class="{{ inputClass|default('bg-white pl1 br2-ns br--left-ns') ~ ' ' ~ width|default }}" type="{{ type|default('text') }}" id="{{ id|default }}" name="{{ name|default }}" value="{{ value|default }}" {{ extraAttrs }} oninput="this.setCustomValidity('')" oninvalid="this.setCustomValidity('{{ validation.option[type|default('text')] }}')" />