# Base dirs configuration
paths:
  # Cache's path
  cache_dir: /app/cache
  # Data files' path
  data_dir: /app/config
  # Document root files' pathr
  htdocs_dir: /web
  # Logs files' path
  logs_dir: /app/logs
  # Uploaded files' path
  uploads_dir: /uploads